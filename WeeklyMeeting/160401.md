
## radical

### 改写 ReadXML 类

将VC6工程下 ReadXML 类中三个函数改写到一个函数，摒除中间从数据库读出写文件过程。

    TemplateCharacter getTemplateCharacterFromBuffer(string xmlcontent, string mark_buffer);
    ScriptCharacter getScriptCharacterFromBuffer(string xmlcontent, string mark_buffer);

### 修复一个编译错误

新增file包含关系错误 [link](http://git.einverne.info/einverne/writing/commit/6726859e49c980af03308c23643aa4b4e49ff554)

### 增加绘图时颜色接口

在 StrokeDrawnode 中增加绘图时的接口，笔段绘制过程需要重新思考。

## 待做

### 分析VC工程下手写字信息

查看手写字 xml 信息，将程序改成成直接使用点集信息内容以适应手机app，待做。

### 如何在app中安排绘图的流程

VC 中将绘图放到 TemplateCharacter 和 ScriptCharacter 中，而在 `Cocos2d-x` 下必须将绘制过程放到 CCNode 子类中，所以原先的绘制过程定义在了  `StrokeDrawnode` 中，如此分开之后会产生很多中间数据的交换。如何安排类的位置需要思考一下。
